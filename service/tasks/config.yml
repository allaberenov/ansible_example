---
- name: Deploy service_state.json
  template:
    src: service_state.json.j2
    dest: /opt/service_state.json
  notify: restart nginx

- name: Deploy nginx config
  template:
    src: nginx.conf.j2
    dest: /etc/nginx/conf.d/service.conf
  notify: restart nginx
